[package]
name = "fm-core"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["full"] }
mime_guess = "2"
infer = "0.16"
libc = "0.2"

[dev-dependencies]
tempfile = "3"
